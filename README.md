TODO:

Openfaas function:
    handle wrong test parameters,
    zip files,
    create plots,
    enable download,

server:
    front:
        handle wrong test parameters,
        correct date,
    back:
        [openfaasurl, host, port, functionname] from args.

